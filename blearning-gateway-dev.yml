spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    org.springframework.cloud.gateway: INFO
    org.springframework.web.reactive: INFO

management:
  endpoint:
    health:
      show-details: always
